[pytest]
asyncio_mode = auto
testpaths = tests
addopts = --basetemp=E:/2026-/PROMPT_IMAGE-VIDEO/backend/.pytest_tmp -m "not slow"
markers =
    slow: marks tests that run real SD inference (deselect with -m "not slow")
